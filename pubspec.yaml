name: table_calendar
description: Highly customizable, feature-packed calendar widget for Flutter.
version: 4.0.0
author: Arkar Min Tun <arkarmintun1@gmail.com>
homepage: https://github.com/arkarmintun1/table_calendar

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.20.0
  timezone: ^0.9.2
  simple_gesture_detector: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.1

flutter:
